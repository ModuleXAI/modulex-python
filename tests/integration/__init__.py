"""Integration tests for the ModuleX Python SDK.

These tests run against a real API and require environment variables:
  MODULEX_API_KEY     — Required
  MODULEX_ORG_ID      — Required
  MODULEX_BASE_URL    — Optional (default: https://api.staging.modulex.dev)
"""
